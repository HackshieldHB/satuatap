# Firmware (placeholders)

FUTURE HARDWARE INTEGRATION — NOT CONNECTED IN CURRENT PHASE.

This folder will hold ESP32 firmware that publishes `satuatap/{homeId}/{deviceId}/telemetry` exactly like `apps/iot-simulator`.
